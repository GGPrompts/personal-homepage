# Supabase (required for auth sync)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Financial APIs (optional - for stocks dashboard)
FINNHUB_API_KEY=your-finnhub-key
ALPHA_VANTAGE_API_KEY=your-alpha-vantage-key

# GitHub (optional - for enhanced GitHub features)
GITHUB_TOKEN=your-github-token

# Google OAuth (required for Gmail/Calendar integration)
# Get these from https://console.cloud.google.com/apis/credentials
# 1. Create a new OAuth 2.0 Client ID (Web application)
# 2. Add authorized redirect URIs:
#    - http://localhost:3001/api/auth/google/callback (development)
#    - https://your-domain.com/api/auth/google/callback (production)
# 3. Enable APIs: Gmail API, Google Calendar API
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3001/api/auth/google/callback

# Optional: Public client ID for client-side OAuth (not recommended for web apps)
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Spotify (optional - for music player)
# NEXT_PUBLIC_SPOTIFY_CLIENT_ID=your-spotify-client-id

# YouTube Data API (optional - for video player search)
# Stored in localStorage, but can also be set here
# YOUTUBE_API_KEY=your-youtube-api-key
